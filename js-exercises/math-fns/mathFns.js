// You can use the function from the `Math` module.
  function sqrt(n){
    return Math.sqrt(n);
  }

  function power(n,p){
    return Math.pow(n,p);
  }

  function round(n){
    return Math.round(n);
  }

// Don't change the exported names. 
export {
  sqrt,
  power,
  round,
};
